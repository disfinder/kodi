---

- name: Bootstrap the box
  hosts: kodi
  gather_facts: no
  become: yes
  pre_tasks:
    - name: Include main vars
      include: include-header.yml
  roles:
    - name: Bootstrap users
      role: bootstrap/user-manager
    # - name: Tune bash for user
    #   role: user/bash
    # - name: Tune screen for user
    #   role: user/screen
    - name: Set up WI-FI
      role: bootstrap/wifi-network